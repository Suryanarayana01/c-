#include<iostream>
using namespace std;

int main(){
    int num, limit;
    cout << "Enter a number and its limit: ";
    cin >> num >> limit;
    for(int i = 1; i <= limit; i++){
        cout << num << " * " << i << " = " << num * i << endl;
    }
    return 0;
}
